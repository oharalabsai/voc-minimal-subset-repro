# Audit — voc-minimal-subset

**Gate: PASSED**

Three independent fresh-context critics, run in parallel (docs/paper-audit-protocol.md).

## prior-art

**Bottom line:** No THREATENS-THE-RESULT finding on my axis. The prior HOLD's single THREATENS item — the 'exhaustive enumeration / provably fails / unique / regardless of dose' overclaim over a space that was only single-config-enumerated — is now RESOLVED: every exhaustive/negative statement is scoped to the single-config space, the multi-config drop-SRPL family is explicitly flagged as argued-not-graded (abstract, §1, §2.3, §3.3, Limitations), and the drop-transmission negative is correctly restricted to the airtight PL-carries-no-Eg/d argument. All quantitative claims reconcile with the hash-chained ledger (leverage 0.31/20.06/47.25 mV; three run-2 hits; H3 9.13 mV; budgets (220,201)->(70,13.5); 68%/93%; near-miss frontier 10.33/14.26/13.24/20.06/64.22/58.02 mV). Physics Eq.(1) is correctly attributed to published sources, not sold as novel; the post-freeze verifier patch is disclosed accurately and is corroborated by byte-identical cross-run finite-dose values. Two PRESENTATION tightenings remain; the paper can close on my axis.

- [PRESENTATION] CONFIRMED: Prior HOLD's THREATENS item: 'certified by exhaustive enumeration', 'provably fails', drop-SRPL 'floors regardless of dose', {SRPL,transmission} 'unique' — over a space that admits ungraded multi-config repeats. — RESOLVED in this draft. Title/abstract/§1/§2.3/§3.3 now scope every exhaustive/negative statement to 'single-config-per-modality' and explicitly state multi-config repeats were NOT brute-forced ('we did not exhaustively grade', 'argued bound, not a graded certification'). §3.3 drops the false 'no config buys it back / regardless of dose' rationale and instead admits 'TRPL repeats do lower the indirect-PLQE variance as sqrt(dose)'. Limitations flags the multi-config drop-SRPL family as the open check. The overclaim that triggered the prior HOLD no longer stands.
- [PRESENTATION] CONFIRMED: The reduction exists: three double-run-certified {SRPL,transmission} passers; H3=SRPL(0.25,50ms)+trans(20ms) recovers full implied V_OC to 9.13 mV worst-case at 68% less time / 93% less dose; leverage TRPL 0.31 / SRPL 20.06 / transmission 47.25 mV. — Reconciles exactly with ledger rows 24-26 (three run-2 search_hit), row 17 (leverage triplet), row 19 (H3 9.13 mV at dose 13.5). Budgets recompute from the pinned accounting: full (220,201), H3 (70,13.5) -> 68.2% time / 93.3% dose. TRPL redundancy is framed 'by construction, expected, not a discovered shortcut' per the contract's VACUOUS-GRID rule. No severity — logged as the positive result's provenance.
- [PRESENTATION] DISPUTED: Abstract: {SRPL,transmission} 'is the only two-modality subset containing a passer'. — The bald abstract phrase is certified only for single configs; it rests entirely on the ungraded drop-SRPL multi-config ({trpl,transmission}) family, since drop-transmission subsets are airtight-excluded. The abstract does immediately qualify ('every ENUMERATED single-config protocol that drops either fails', 'we scope the negative accordingly'), and the body (§4) correctly states the 'only droppable modality' reading is 'certified for single configs and argued for repeats'. Tightening only: add 'single-config' to the 'only subset' phrase in the abstract so it does not read forward of its own hedge. Not THREATENS — the existence headline does not depend on it and the prior critic's own estimate (~14 mV) has the drop-SRPL negative surviving.
- [PRESENTATION] NOT-FOUND: Reference louks2025 = arXiv:2508.21037 (Louks, A. and others, 2025); the ~170 mV implied-to-contacted offset attributed to it. — COULD NOT independently verify this session — WebFetch/WebSearch permission not granted (⚠️ arxiv.org/abs/2508.21037 unreachable). It is the campaign's pinned premise (present in CONTRACT.md/DIRECTION.md at setup), not writer-fabricated, and the .bib entry itself self-flags 'Author list and the ~170 mV offset to be confirmed against the arXiv record before publication'. Nothing in the draft misattributes a result to it beyond the offset it already frames as that paper's report. Before publication: confirm the arXiv id resolves, replace 'A. and others' with the real author list, and confirm the source reports the ~170 mV offset.
- [PRESENTATION] CONFIRMED: Eq.(1) implied V_OC = V_OC,rad - kT|ln PLQE| and the detailed-balance / Urbach-tail construction are presented as the published model, not a novel contribution. — No textbook-as-novel problem. Eq.(1) is attributed to ross1967/rau2007; detailed balance to shockleyqueisser1961; the Urbach tail to urbach1953 — four canonical, genuine references (correct venue/volume/DOI per prior panel cross-check). The paper's novelty is scoped modestly to the information-sufficiency / dose-frontier analysis of one pinned model; it makes no 'first'/'resolves-open-question' literature claim. No overclaim.
- [PRESENTATION] CONFIRMED: §2.5/§3.4: the sealed verifier code was patched after the contract freeze; 'the finite-dose G1 grading path never reaches the floor, so the graded worst-case values are unchanged'. — Disclosure is accurate and now explicit that contract_sha pins CONTRACT.md, not verifier.py, and that the oracle source received post-freeze reviewer patches (fusion sigma floor, np.trapezoid rename, candidate-parse fallback, all 2026-08-26). The 'finite-dose unchanged' claim is corroborated by the ledger's own before/after values: srpl(0.1,100)+trans(20) = 10.33 mV in both run 1 (row 6) and run 2 (row 19); drop-SRPL = 20.06 mV in both (rows 9, 23). The run-1 false positives (rows 13-15) flipped fail->pass only at the infinite-dose dress-rehearsal stage, which the contract's DRESS-REHEARSAL kill criterion sanctions fixing. Honest; only a spot-check (two matched points) rather than exhaustive, but presented as such ('e.g.').

## reproduction

**Bottom line:** REPRODUCED — clean. Every quantitative claim in the draft matches the ledger verdicts, and all derived numbers recompute correctly. Leverage (TRPL 0.31 / SRPL 20.06 / transmission 47.25 mV), H3 worst-case 9.13 mV, all seven near-miss/frontier values (10.33, 14.26, 13.24, 20.06, 64.22, 58.02 mV), all budgets (full 220/201; H1 120/101; H2 70/51; H3 70/13.5), the 68% time and 93% dose reductions, the run stats (run1: 229 cand / 3 claimed / 0 verified / 7 near-miss; run2: 161 / 3 / 3 / 5), and the 137-candidate single-config enumeration count all trace to the ledger and recompute exactly. Scope is honestly bounded to the pinned model's own implied V_OC; the ~170 mV device offset is disclosed as out of scope; the synthetic/NONE-ATTESTED grid is presented as synthetic with no real-world overclaim; the run-1 verifier-patch (byte-identity fix) is disclosed and only post-fix run-2 verdicts are certified as hits. No THREATENS-THE-RESULT finding. NOTE: the verdict block supplied to this auditor was empty; I reproduced against ledger.jsonl and re-derived percentages/budgets myself.

- [PRESENTATION] REPRODUCED-AS-BOUND (not a discrepancy): Table 2 lists H1 (SRPL 1.0,100ms) and H2 (SRPL 1.0,50ms) with 'Worst-case ≤ 9.13 mV'. — The ledger's search_hit records for H1 (hit_index 0) and H2 (hit_index 1) carry only valid:True — no per-hit worst-case mV. Only H3's 9.13 mV is a recorded sealed output (near-miss note: 'dose 13.5 ... passes at 9.13 mV'). The '≤ 9.13' for H1/H2 is an argued bound from the observed monotonicity of error in SRPL dose (points 5→14.26, 10→10.33, 12.5→9.13). The draft flags this explicitly ('the near-miss series ... establishes monotonicity, so "≤" is a sound bound') and both hits are independently machine-verified valid:True (they pass G1 ≤10 mV regardless of exact value). Honest as written; the '≤' and footnote should stay so a reader does not read 9.13 as H1/H2's measured value.
- [NICE-TO-HAVE] REPRODUCED: Draft §3.3: verifier.py received post-freeze reviewer patches (NaN-fusion floor, trapezoid rename, parse fallback); run-1 hits failed byte-identity and were recorded as non-hits, run-2 re-certified them. — Confirmed against ledger: run1 hits_claimed=3 with three search_false_positive rows (0 verified); run2 three search_hit rows (3 verified). Run2 dead_ends explicitly states run1's claimed hits were byte-identity failures from the pre-fix _fuse NaN bug, re-certified by the fixed verifier. Engine integrity confirms harness_sha and contract_sha constant across all rows (0 harness drift, 0 chain breaks). The disclosure is accurate. Only refinement: the draft should keep making unmistakable that the certified hits (Tables 1–2, Figs) are all run-2 (post-fix) verdicts, since run-1 verdicts for the same candidates are not ledger hits.
- [NICE-TO-HAVE] REPRODUCED / no overclaim: Scope + data provenance: pure information-sufficiency vs the pinned model's own implied V_OC; ~170 mV device offset out of scope; synthetic 24-device grid. — Engine reports dataset_source NONE ATTESTED (synthetic). The draft makes NO real-world / measured-JV claim — it repeatedly and correctly bounds every conclusion to 'this model and this grid draw' and labels the grid synthetic in abstract, intro, and limitations. This is the correct handling of a synthetic/none-attested campaign, not a defect; recording it so the panel sees the trap was avoided.

## honesty

**Bottom line:** PROCEED (no THREATENS-THE-RESULT findings). The headline is scoped exactly to the evidence: three double-run-certified {SRPL,transmission} passers back the 'reduction exists' claim, all mV are real sealed-verifier outputs (confirmed in verifier.py and the ledger), the byline is the two approved humans with a clean AI-contribution statement, and the negative result is reported as a co-equal — with the single-config-exhaustive vs. multi-config-argued boundary disclosed in ≥4 places, which resolves the prior audit HOLD. The skeptic's first attack (oracle patched after the freeze) is disclosed and correctly rebutted: the finite-dose grading path is provably unchanged (the 1e-12 fusion floor never triggers at finite dose) and the near-miss mV are byte-identical across runs. The synthetic/NONE-ATTESTED dataset is not overclaimed — every conclusion is scoped to the pinned forward model, not real-device JV Voc. A few PRESENTATION cleanups below; none block closure.

- [NICE-TO-HAVE] CLEAN — authors are Gyalpo Dongo and Sebastian Alberdi (the project's approved human authors per byline law); no AI is named as author; the AI-contribution statement correctly casts the harness as executing mechanical steps under human direction and states all numbers are verifier verdicts.: Byline law and AI-contribution statement — Confirmed against CLAUDE.md byline law. No action needed.
- [NICE-TO-HAVE] CONFIRMED. verify() returns worst_mv in both the reason string and details on pass and fail; ledger reason strings carry 64.22/20.06/58.02/10.328/13.24/14.26 mV; leverage {srpl:20.06, trpl:0.31, transmission:47.25} matches the setup ledger; 137-candidate single-config count reconciles exactly (9+9+2+81+18+18). Exhaustiveness claim over single configs verified.: All reported mV figures (9.13 pass margin, 20.06/47.25 leverage, near-miss frontier) are sealed-verifier outputs, not self-reported — Rule-3 (nothing self-reported) satisfied — the worst-case mV is a genuine oracle field.
- [NICE-TO-HAVE] NOT A VIOLATION. The engine's synthetic-data THREATENS trigger fires only if the run is sold as real-world evidence; the draft does the opposite — abstract, intro, discussion and Limitations repeatedly restrict every claim to the pinned forward model's OWN implied Voc and explicitly disclaim the ~170 mV implied-to-contacted device offset as out of scope. No scope drift.: Scope discipline: synthetic model output, not real-device Voc (dataset_source NONE ATTESTED)
- [NICE-TO-HAVE] RESOLVED. The overreach the earlier panel flagged ('provably fails'/'certified by exhaustive enumeration' applied to the whole space) is gone: the abstract, Contribution, §4.3, Discussion and Limitations all distinguish single-config-exhaustive (certified) from multi-config repeats (transmission-drop airtight against repeats by a no-Eg/d-in-PL argument; SRPL-drop bounded by variance-scaling, explicitly not brute-forced). Rejections and the run-1 false positives are reported prominently, not buried.: Negative-result honesty (the prior audit HOLD): the certified negative is scoped to single-config protocols, with multi-config drop-SRPL argued (not graded)
- [PRESENTATION] DISCLOSED AND SOUNDLY REBUTTED. §4.5 states the patch, notes the SHA pins CONTRACT.md not verifier.py, and defends via byte-identical finite-dose numbers across runs (10.33 mV and 20.06 mV in both). Verified in code: the reviewer fix floors fusion sigma at 1e-12, which finite-dose grading never reaches, and the trapezoid-rename/parse-fallback changes touch no graded numeric — so the patch cannot have manufactured a passer. Keep this section as prominent as it is.: First skeptic attack — the oracle (verifier.py) was patched after the contract freeze, and the three hits only entered the ledger post-patch; the contract SHA does not cover the oracle source — The defense is legitimate; recommend keeping the 'contract SHA does not pin the oracle source' sentence — it is the honest disclosure that makes the byte-identity argument load-bearing.
- [PRESENTATION] IMPLICIT INACCURACY (does not threaten the result). If dress_rehearsal returned NaN, _REFEREE_OK would be False and verify() would RAISE for EVERY candidate — yet run-1's ledger records finite-dose near-misses (10.33, 20.06 mV), so the referee was NOT globally raising in run 1. The bundled candidate-parse fallback (single-quoted repr not parsed by json.loads → the engine's double-run rejecting a candidate the searcher accepted) is the more plausible byte-identity culprit. Either way the disclosed facts hold (patch disclosed; graded numbers unchanged; hits entered only in run 2), so this is a factual-precision nit in the integrity narrative, not a hidden failure.: §4.5 root-cause attribution: run-1 byte-identity failures blamed on the _fuse NaN at the infinite-dose dress rehearsal — Suggest softening to 'a bundle of referee bugs (fusion NaN at the dress-rehearsal limit and a candidate-parse mismatch on re-verification)' rather than pinning the double-run failure solely on the NaN.
- [NICE-TO-HAVE] SLIGHTLY LOOSE but self-corrected. A srpl(dose 50)+trans(5 ms) passer at 8.48 mV is noted in the run-2 near-miss text (55 ms time / 50.25 dose) and does not Pareto-dominate H3 (70 ms / 13.5 dose) — the two trade time vs dose. The Limitations section correctly states H3 is dose-minimal and NOT claimed unique cost-optimal, and it is the cheapest of the three ledger-recorded hits. Recommend the abstract say 'the dose-minimal certified passer' to match the body.: Abstract calls H3 'the cheapest certified passer'
- [NICE-TO-HAVE] DISCLOSED; low risk to the result because every campaign claim is about the pinned forward model in verifier.py (self-contained), which the real paper only motivates. Still, confirm the author list and the 170 mV figure against the live arXiv record before publication. (Primarily Agent A / prior-art territory.): The anchor citation louks2025 (arXiv:2508.21037) carries a .bib note that its author list and the ~170 mV offset are unconfirmed against the arXiv record

## design-skeptic

**Bottom line:** The estimand is honest and the design is clean: this is a well-scoped information-sufficiency question against a pinned forward model's OWN implied V_OC, correctly baselined, with no leakage and no confound in the modality-leverage measurement. No THREATENS-THE-RESULT finding. The existence claim ('a <=2-modality reduction exists; TRPL is redundant') is oracle-certified and robust. Two PRESENTATION issues: (1) the flagship H3 number (9.13 mV, '93% less dose') conflates the modality-reduction effect (~0.31 mV, the certified TRPL leverage) with a self-imposed 8x SRPL-dose cut (~8.8 mV) that has nothing to do with 2-vs-3 modalities; and (2) H3's 0.87 mV margin is a worst-case over only 20 noise seeds/device and is the seed-fragile part of the result. Both are disclosed in part but should be sharpened.

- [PRESENTATION] the modality-reduction effect (TRPL redundant, ~0.3 mV) is real and certified; the 9.13 mV framing attributes to '2 vs 3 modalities' an error that is ~97% the cost of cutting SRPL dose: Headline metric bundles two independent effects. H3's 9.13 mV worst-case = drop-TRPL cost (~0.31 mV, the certified leverage floor) PLUS an 8x SRPL-dose cut (100->12.5, ~2.83x noise blow-up on direct PLQE, ~8.8 mV). The ledger confirms G1 worst-case is f(SRPL dose, transmission dose) only, monotone in SRPL dose down to the 0.31 mV TRPL-leverage floor. H1 (SRPL 1.0/100ms + trans 20ms) is literally 'full protocol minus TRPL' and therefore recovers to sub-mV, yet Table 2 lists it as '<= 9.13 mV', obscuring that it is the robust, mechanism-clean demonstration of the redundancy claim. — Report H1's actual worst-case (near the 0.31 mV floor) rather than '<= 9.13 mV', and separate the two levers: dropping TRPL costs ~0.3 mV; the extra budget savings come from lowering SRPL dose, which is a precision tradeoff independent of the modality question. This strengthens, not weakens, the paper — the redundancy is far more robust than 9.13/10 suggests.
- [PRESENTATION] the 'exists' headline is robust via H1 (~9 mV of headroom); the 'cheapest passer at 93% less dose' claim specifically rests on a tight, seed-sample-limited margin: Cheapest-passer margin is a 20-seed extreme-value estimate. H3 clears the bar by 0.87 mV, but the worst-case is taken over only 24 devices x 20 certification seeds. An extreme-value statistic from 20 noise realizations per device under-estimates the true worst-case over the noise distribution; a resampled or larger cert-seed block could push H3 over 10 mV. — Paper half-discloses ('H3 sits 0.87 mV under the bar... property of this grid realization'). Make explicit that the existence/redundancy claim does not depend on H3's margin, and that H3's 0.87 mV headroom is a 20-seed worst-case, not a distributional bound.
- [PRESENTATION] adequately scoped in the draft (Limitations: 'single random draw', 'conclusions hold for this model and this grid draw', 'non-load-bearing by construction'): Scope of inference: one grid draw, redundancy is assumption-driven not sample-estimated. n = 24 devices from a single fixed parametric box (GRID_SEED=20250827); the 20 cert seeds add measurement-noise diversity, NOT independent materials, so the effective independent physical unit count is 1 grid realization / 1 forward model / 1 pinned noise parameterization. The 'TRPL redundant' conclusion is a direct consequence of the pinned 6.7x SRPL/TRPL precision ratio (sigma 0.03 vs 0.20), not an empirical discovery. — Checked: the redundancy is analytic-by-construction, correctly attributed as such per the contract's VACUOUS-GRID handling, not sold as a discovered shortcut. Keep population phrasing ('for realistic stacks') explicitly tethered to the pinned noise ratio; the leverage check is correctly named as the guard that would flag a better TRPL route or a wider grid.
- [NICE-TO-HAVE] design isolates the claimed effect; no confound, no circularity, no leakage path: Clean bill on leakage, baseline validity, and estimand construction (what I checked). (a) Leakage: the oracle grades only on CERT_SEEDS and the entire <=2-modality single-config space is exhaustively enumerated and graded by the sealed oracle — this is existence-certification, not train/test generalization, so 'cert seeds used in selection' is not a contamination (there is no disjoint held-out set the winner must generalize to; the cert seeds ARE the evaluation). (b) Baseline: ground truth is the full protocol's OWN output with retained modalities carrying identical noise draws (_z keyed by seed/device/quantity, independent of dose), so shared modalities cancel exactly and the graded difference isolates the dropped modality's contribution. (c) Estimand faithfulness: dress_rehearsal confirms infinite-dose recovery of the true implied V_OC to <1e-6 V; the winning {srpl,transmission} subset never touches the LODO-prior machinery (SRPL->PLQE, transmission->Eg/d/EU). (d) Synthetic-by-design and correctly scoped: the paper presents results only as information-sufficiency against the pinned model and puts the ~170 mV real-device offset explicitly out of scope, so 'dataset_source NONE ATTESTED / SYNTHETIC' is appropriate and not an unsupported real-world claim. — Also checked the post-freeze verifier patch (_fuse NaN floor, trapezoid rename, parse fallback): the ledger shows finite-dose G1 values byte-identical across runs (near-miss 10.33 mV in both run 1 and run 2), consistent with the engine's 0 harness-drift / 0 chain-break count. The patch touches only the infinite-dose dress-rehearsal path and does not change what was measured. Disclosure in Section 3.3 is accurate.

## engine (mechanical)

**Bottom line:** Deterministic checks the engine performs itself — no model judgment involved.

- [PRESENTATION] NOT-FOUND: Byline whitelist unavailable: config/policy.yaml is missing — Copy config/policy.example.yaml to config/policy.yaml and list the approved human authors; until then the byline cannot be verified mechanically.
